{
  "name": "proyecto-final-master",
  "version": "1.0.0",
  "description": "Segunda entrega de proyecto final de coderhouse con Bootstrap y animaciones.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss scss/main.scss css/reducido.css",
    "watch-css": "nodemon -e scss -x \"npm     run build-css\"",
    "build":"purgecss --css css/styles.css --content index.html -o css/reducido.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
